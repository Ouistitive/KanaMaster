<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.KanaMaster"
        tools:targetApi="31">
        <activity
            android:name="activites.ClassementActivity"
            android:exported="false" />
        <activity
            android:name="activites.ScoreActivity"
            android:exported="false" />
        <activity
            android:name="activites.JeuActivity"
            android:exported="false" />
        <activity
            android:name="activites.InscriptionActivity"
            android:exported="false" />
        <activity
            android:name="activites.MainActivity"
            android:exported="false" />
        <activity
            android:name="activites.ConnexionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>